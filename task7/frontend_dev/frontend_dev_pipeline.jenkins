pipeline {
    agent any

    stages {
    
        stage('build') {
            steps {
                dir('task7/frontend_dev/'){
         		sh 'docker build --no-cache -t radiolacattor/frontend_dev .'
                sh 'docker stop frontend_dev || true'
                sh 'docker run --rm --name frontend_dev -d -p 4300:80 radiolacattor/frontend_dev'
           		}
            }
        }
    }
}