pipeline {

agent any

environment {
     PASSWORD = credentials('pass')
}

stages {
    stage('build'){
        steps {
            dir ('task4') {
                sh 'docker build -t rad:lts .'
                }
            }
        }
    }

}
