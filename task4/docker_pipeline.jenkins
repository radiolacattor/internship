pipeline {

agent any

environment {
    PASSWORD = credentialsId('PASSWORD')
}

stages {
    stage('build'){
        steps {
            dir ('task4') {
                sh 'docker build -t rad:lts .'
            	}
            }
        }
    }
}
