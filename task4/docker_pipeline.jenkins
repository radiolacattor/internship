pipeline {

agent any

environment {
     PASSWORD = credentials('PASSWORD')
}

stages {
    stage('build'){
        steps {
            dir ('task4') {
                sh 'docker build -t rad:lts .'
                }
            }
        }
    }

}
