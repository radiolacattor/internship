pipeline {

agent any

stages {
    stage('build'){
 	  	environment {
    		PASSWORD = credentials('PASSWORD')
}
        steps {
            dir ('task4') {
                sh 'docker build -t rad:lts .'
                }
            }
        }
    }

}
